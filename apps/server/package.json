{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "denon run -A src/index.ts",
    "start": "deno run -A src/index.ts",
    "build": "deno run -A src/index.ts --build",
    "lint": "deno lint --unstable --ignore=dist",
    "format": "deno fmt --ignore=dist",
    "install-denon": "deno install -qAf --unstable https://deno.land/x/denon/denon.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint-config-custom": "*",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  }
}
